@model ColpatriaSAI.UI.MVC.Areas.Comisiones.Models.ViewModels.ParametrosVencimientoViewModel
@{
    ViewBag.Title = "Comisiones - Parámetros Vencimiento";
    Layout = "~/Areas/Comisiones/Views/Shared/_comisionesLayoutPage.cshtml";
}

<h2>Parámetros Vencimiento</h2>

<div class="container">
    <div class="row">
        <div class="col-sm-4 text-right">
            <label>
                Parametrización Vencimiento TEST!!!</label>
        </div>
        <div class="col-sm-4">
            
        </div>
    </div>
    @*<div class="row">
        <div class="col-md-6">
            <input type="button" value="Aceptar" onclick="javascript:$('#dialog-tipo-contratos').dialog('open');" />
        </div>
    </div>*@
</div>
@*<div id="dialog-form" title="Rotar Históricos">
    @using (Html.BeginForm("Agregar", "Parametrizacion", FormMethod.Post, new { area = "Comisiones", @class = "form-horizontal", role = "form" }))
    {
        @Html.HiddenFor(m => m.IdModelo)
        <fieldset>
            <legend></legend>
            <div class="form-group">
                <label class="col-sm-4 text-right">
                    Compañ&iacute;a:
                </label>
                <div class="col-sm-8">
                    @Html.DropDownListFor(m => m.IdCompaniaSeleccionada, Model.Companias, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 text-right">
                    Ramo:
                </label>
                <div class="col-sm-8">
                    @Html.DropDownListFor(m => m.IdRamoSeleccionado, Model.Ramos, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 text-right">
                    Producto:
                </label>
                <div class="col-sm-8">
                    @Html.DropDownListFor(m => m.IdProductoSeleccionado, Model.Productos, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 text-right">
                    Plan:
                </label>
                <div class="col-sm-8">
                    @Html.DropDownListFor(m => m.IdPlanSeleccionado, Model.Planes, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 text-right">
                    Tipo de Contrato:
                </label>
                <div class="col-sm-8">
                    @Html.DropDownListFor(m => m.IdTipoContratoSeleccionado, Model.TipoContratos, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 text-right">
                    Factor(%):
                </label>
                <div class="col-sm-8">
                    <input type='number' class="form-control number" />
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-12 text-center">
                    <input type="submit" value="Agregar" />
                </div>
            </div>
        </fieldset>
    }
</div>*@
